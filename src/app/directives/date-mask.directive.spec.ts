import { DateMaskDirective } from './date-mask.directive';

describe('DateMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new DateMaskDirective();
    expect(directive).toBeTruthy();
  });
});
