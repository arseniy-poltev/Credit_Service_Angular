import { LimitnumberDirective } from './limitnumber.directive';

describe('LimitnumberDirective', () => {
  it('should create an instance', () => {
    const directive = new LimitnumberDirective();
    expect(directive).toBeTruthy();
  });
});
